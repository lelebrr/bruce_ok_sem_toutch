Por favor, coloque todos os arquivos de cabeçalho das imagens (os arquivos `.h` que contêm os dados dos seus ícones e imagens) neste diretório.

O código principal em `src/main.cpp` irá incluí-los a partir daqui.
